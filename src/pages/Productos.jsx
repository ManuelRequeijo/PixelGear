// Productos.jsx — La página con el listado de todos los productos (ruta "/productos")
// Usa el componente ItemListContainer para mostrar los productos.

import ItemListContainer from "../components/item/ItemListContainer"

function Productos() {
  return (
    <div>
      <ItemListContainer />
    </div>
  )
}

export default Productos
